name: Build Backend Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: docker

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install Docker CLI
        run: |
          apt-get update
          apt-get install -y docker.io

      - name: Check Dockerfile
        run: |
          ls -la
          test -f Dockerfile

      - name: Build image
        run: |
          docker build -t ppklj-backend:ci .
